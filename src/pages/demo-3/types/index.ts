export interface SpecItem {
  tag: string;
  value: string;
  unit: string;
  label: string;
  highlight: boolean;
}

export interface GalleryItem {
  src: string;
  label: string;
  tag: string;
  scrubY: number;
}
